throw = {1,...,20}
face = {1,...,6}

Outcome(throw, face)




Outcome(t,1) v Outcome(t,2) v Outcome(t, 3) v Outcome(t, 4) v Outcome(t, 5) v Outcome(t, 6).

// !Outcome(t,f0)  v !Outcome(t,f1) v f0 = f1.

Outcome(t,f0) ^ !(f0 = f1) => !Outcome(t,f1).
